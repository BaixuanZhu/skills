# 回归盲评 · java-integration-test v1.2.0（工作区版本）

> **日期**：2026-08-19
> **盲评员**：C（`blind-C.md`，95.2/100）· D（`blind-D.md`，96.0/100）
> **基线**：v1.0.0 双盲评 A 93.5 / B 94.6（2026-08-14，`blind-summary.md`）
> **目的**：v1.0.0 → v1.2.0 之间多轮修改（Boot 4 工具链适配 / mock 注解全家桶迁移 / 依赖坐标核查 / checklist 化）后的达尔文回归——验证无退步、无自相矛盾。

## 一、回归背景（被评版本相对 v1.0.0 的改动面）

1. **Boot 4 工具链**：新增 RestTestClient（替代 TestRestTemplate，含 `spring-boot-resttestclient` 坐标与 starter 讹传警告）、Testcontainers 2.x 模块改名/包重定位/无泛型对照表、`@SpringBootTest` 不再自动配 MockMvc。
2. **mock 注解全家迁移**：`@MockBean`/`@SpyBean`（Boot 4 已移除）→ `@MockitoBean`/`@MockitoSpyBean`，全套件 33 处；02 一处定义版本口径，其余文件指针。
3. **依赖版本分流**：rest-assured（≤3.x 走 BOM / 4 手写 6.0.1）、JUnit（Boot ≤3.x = 5、4 = 6，不手选）、TC Docker v29 需 ≥1.21.4（Boot 3.5 BOM 管 1.21.3 命中）。
4. **checklist 化**：SKILL.md 新增「输出前自检清单」9 条 checkbox（任一未过不得交付）。
5. 全部版本口径改为「条件→唯一结论」映射（无"双版本都行"式模糊）。

## 二、双盲评结果与基线对比

| 盲评员 | 版本 | 总分 | 判定 |
|---|---|---|---|
| A | v1.0.0 | 93.5 | 通过 |
| B | v1.0.0 | 94.6 | 通过 |
| **C** | **v1.2.0** | **95.2** | **通过** |
| **D** | **v1.2.0** | **96.0** | **通过** |

**结论：无退步，均分 +1.55。** 两份新盲评 10 prompt 全部 ≥91，分歧 ≤2 分（T10：C 91 / D 97，源于 D6 判罚松紧，与 v1.0.0 轮 A/B 的分歧模式一致）。

### 维度均分对比（本轮盲评 C/D vs 基线 A/B）

| 维度 | A(v1.0.0) | B(v1.0.0) | C(v1.2.0) | D(v1.2.0) | 信号 |
|---|---|---|---|---|---|
| D1 触发精度 | 11.2 | 11.1 | 11.1 | 11.6 | 持平 |
| D2 可发现性 | 11.2 | 11.6 | 11.7 | 11.9 | ↑ |
| D3 覆盖完整 | 11.8 | 11.9 | 11.9 | 12.0 | Boot 4 维度补齐无盲点 |
| D4 可执行性 | 11.6 | 11.5 | 11.6 | 11.8 | 持平 |
| D5 防错/陷阱 | 11.5 | 11.4 | 11.6 | 11.8 | ↑ |
| **D6 信息密度** | **10.0** | **10.7** | **10.5** | **9.4** | **仍为共同最低维度** |
| D7 内部导航 | 11.0 | 11.2 | 11.6 | 12.0 | ↑（checklist + 版本指针） |
| D8 范围明确 | 11.2 | 11.2 | 11.2 | 11.5 | 持平 |
| **D9 整体一致性** | 4.0 | 4.0 | **4.0** | **4.0** | **满分保持——大改后无自相矛盾** |

**本轮修改的核心验证点达成**：D9 四份盲评满分趋同——33 处注解迁移 + 多文件版本口径插入后，全套件术语/规则/版本号无自相矛盾；D3/D5 上浮来自 Boot 4 工具链覆盖补齐（此前 Boot 4 用户会拿到已失效的 TestRestTemplate/@MockBean/TC 旧坐标）。

## 三、盲评发现问题与仲裁

### 代码级缺陷（存量，v1.0.0 实跑未覆盖到的示例，非本轮修改引入）

| # | 位置 | 问题 | 点名 | 仲裁 | 处置 |
|---|---|---|---|---|---|
| 1 | `references/07` L354 | `@Test` 标注在 class 上（JUnit 5 `@Target` 仅 METHOD，编译报错） | C+D 双确认 | **实锤** | ✅ 已修（删除类上注解） |
| 2 | `references/06` L69-73 | `@AfterEach` 用未声明的 `jdbcTemplate` 字段，照抄编译不过 | D | **实锤** | ✅ 已修（补 `@Autowired JdbcTemplate jdbcTemplate;`） |
| 3 | `references/08` L121-122 | 类级 `@Timeout(30)` 被判「JUnit 5 @Target 无 TYPE，照抄编译失败」 | C | **误报**——本机 junit-jupiter-api 6.0.3 字节码实证 `@Target` 含 `TYPE`（JUnit ≥5.9 支持类级，Boot 3.x/4.x 传递版本均满足） | ⚠️ 保留代码，补版本注（Boot 2.7 的 JUnit 5.8 仅方法级） |

### D6 残余重复（两份盲评共同点，与 v1.0.0 轮结论同构）

- C：SKILL.md 强约束 8 内联 08 细节约六成；版本口径句在 01/03/06/07 四文件头逐字重复；快速入门表与路由表同构。
- D：`@Transactional` 根因 3 处全文重述；04 内 static 容器规则+代码两遍；curl 禁令 9 处。
- **处置**：与 v1.0.0 轮 P1-P3 同性质（表达层冗余，非功能缺陷）。四文件头的版本口径句是本轮为 mock 注解迁移新加的——权衡后保留（每文件一句、agent 单读该文件时必需），但在下轮 D6 精简时评估是否可收成 03 一处 + 其余指针。不阻塞本轮回归结论。

### 单点建议（未处置，记录备查）

- D：08 缺 `-Dtest` 类名拼错时 "No tests matching pattern" 的兜底提示；04 的 Kafka 无 `@ServiceConnection` 示例。

## 四、结论

**v1.2.0 通过回归双盲评**（C 95.2 / D 96.0，较基线 +1.55，10/10 prompt ≥91）。Boot 4 工具链适配、mock 注解迁移、checklist 化均未引入退步；D9 满分证明大改后全套件自洽。盲评揪出 2 处存量示例代码缺陷（已修）+ 1 处误报（已仲裁并实证）。D6 仍为唯一系统性短板，属下轮精简候选，不影响 agent 产出合格结果。
