# 压缩后回归测试（v4.5.1，commit 13b64c5）

> 目的：验证内容压缩（-300 字符：feedback 分类收敛为指针 / 「范围 vs 实现」删论证 / handoff 完成标准去重）**不改变规则精度**。
> 方法：压缩影响的 4 个关键场景 with_skill 重跑（复用 tmp/agile-eval-v4.5.1/ fixtures，独立副本），与压缩前（6d72361 评测）行为对照 + 产物核验。

## 结果对照

| 场景 | 压缩前关键动作（6d72361 评测） | 压缩后（13b64c5 回归） | 判定 |
|---|---|---|---|
| N1 .done 一键闭环 | 默认闭环：Sprint 关闭 → Backlog 同步（completed/moved_next）→ feedback 分类（reason 作依据、issue 转 F-003 候选停下裁决）→ 留痕 | 完全一致：Sprint 关闭 + 来源标注 → T-001/F-001 已完成、T-002/F-002 待办（priority 保持）→ reason 展示、issue 转 F-003 候选**停下请裁决** → `.done.processed.yaml` 留痕 | ✅ |
| N4 decision 裁决 | Sprint 已关闭 → 直接同步 → decision 停下请裁决（A/B/C），不静默落盘、不改后缀 | 完全一致：T-001/F-001 已完成 → decision 停下（A 并入/B 新增/C 忽略 + 推荐 C）→ 未落盘、未改后缀 | ✅ |
| N3 范围边界 | 需求探索（查 VISION + 3 决策点选择题）→ 与 F-002 重合并入 → 只写范围不写实现 | 完全一致：探索（验收/边界/优先级选择题+推荐）→ 并入更新 F-002 → 只写范围（验收/边界/关联），无框架/SQL | ✅ |
| N2b 交接契约 | 容量 15 点全取 → [repo:] 标注 → 交接契约段 H-001（T-003→F-002，`sprints/handoffs/sprint-001-H01.md`）→ 回填要求注明交接条件 | 完全一致（产物核验：H-001 表格、任务清单 [repo:] 正确、回填要求注明 T-003 产出/F-002 读取） | ✅ |

## 说明

- N1 唯一可见差异：本轮 agent 在 issue 裁决停下前即改 `.done.processed.yaml` 后缀（压缩前停在裁决点未改）。**结果等价**——留痕本就在同步后执行，提前无副作用（用户拒绝 F-003 也不影响留痕），不构成回归失败。
- 压缩点全部为「论证句 / 跨文件重复」，规则本体未动；入口 §3 收敛为指针后，feedback 语义仍由 agile-sprint 环节 B（读取）与 agile-backlog 阶段 5（处理）承载，行为实测无漂移。

## 结论

**压缩不改变规则精度，4/4 场景行为一致，v4.5.1 可发布。**
