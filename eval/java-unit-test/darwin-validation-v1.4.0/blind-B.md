# 盲评结果（blind-B）

> 独立盲评 agent 打分。版本 A 的 frontmatter `version=1.4.0`，版本 B 的 `version=1.3.2`，但按 rubric 要求**不据此推断优劣**，只按字面内容打分。
>
> 阅读范围：两版的 `SKILL.md` + `01/02/03/04/05/06` 全部 reference。
>
> 跨版本文件比对结论（用于解释为何 T1-T8 多为同分）：
> - `01/02/03/04` 两版**逐字节相同**（`diff` 无输出）。
> - `05` 两版有差异：版本 A 的「三个停止信号」被精简为指向 SKILL.md「DoD 锚点」的一句指针 + 一个判定陷阱；版本 B 仍保留完整三条信号 + 「怎么检查」。
> - `06` 仅 §1 两处 XML 注释里的断言触发措辞不同（版本 A 写"集合内容断言/字段分组断言（同一逻辑组）"，版本 B 写"3+字段断言/集合内容断言"）。
> - `SKILL.md` 差异较大：断言库策略、S 级 @MockBean 行、是否新增「DoD 锚点」小节、快速入门/路由表的指针措辞。

---

## T1 — JaCoCo 集成

### 版本 A
- D1: 11  D2: 11  D3: 11  D4: 12  D5: 11  D6: 11  D7: 11  D8: 11  D9: 4
- 小计: 93/100
- 关键依据:
  1. `05-coverage-and-quantity.md` L38-58：完整 JaCoCo `jacoco-maven-plugin` XML 块，含 `prepare-agent` + `report` execution，可直接复制。
  2. `05` L60：`mvn test` 后看 `target/site/jacoco/index.html`，给出具体查看路径。
  3. `05` L62：点名隐蔽坑"测试不过时报告不生成（report 绑 test phase，BUILD FAILURE 中断）"，并给解法（改 verify 或 `mvn jacoco:report`），D5 命中。
- 扣分主因: 无明显失分。`05` L60 强调"仅当用户问覆盖率且项目无时才提"，触发节制但符合；可发现性需从 SKILL.md L78 路由表跳到 05（一步），可接受。

### 版本 B
- D1: 11  D2: 11  D3: 11  D4: 12  D5: 11  D6: 11  D7: 11  D8: 11  D9: 4
- 小计: 93/100
- 关键依据:
  1. `05` L38-58：与 A 完全相同的 JaCoCo XML 块（05 在 JaCoCo 接入章节两版逐字节一致）。
  2. `05` L60：同 A，给 `target/site/jacoco/index.html` 查看路径。
  3. `05` L62：同 A 的"测试不过报告不生成"坑与解法。
- 扣分主因: 无明显失分。T1 不触及三处改动，两版内容等价 → 同分。

---

## T2 — 遗留代码补测（400 行 Service，static + new 满地）

### 版本 A
- D1: 11  D2: 11  D3: 12  D4: 11  D5: 12  D6: 11  D7: 11  D8: 11  D9: 4
- 小计: 94/100
- 关键依据:
  1. `05` L96-108「遗留代码：先特征测试锁现状，再小步重构」：固定解法顺序（特征测试 → 重构 → 补四维度），并给 characterization test 写法 `assertEquals(currentActual, service.doX(input))`。
  2. `05` L110「禁止 PowerMock / 反射强 mock 绕过 final/static/new」，点名反模式。
  3. `06` L74-91「头号陷阱：被测类内部 new 的对象不能 mock」+ 重构为构造器注入的 ✓/✗ 对照代码块。
- 扣分主因: `05` L106「切片策略」一句较泛，未给"先挑哪个方法"的机械判据（只说"变更频率×业务风险最高"，矩阵在 L66-75，需跳转）。轻微 D4。

### 版本 B
- D1: 11  D2: 11  D3: 12  D4: 11  D5: 12  D6: 11  D7: 11  D8: 11  D9: 4
- 小计: 94/100
- 关键依据: 同 A（L96-110 遗留代码段两版逐字节相同；06 头号陷阱段相同）。
- 扣分主因: 同 A。T2 不触及三处改动 → 同分。

---

## T3 — review checklist（提交前自查）

### 版本 A
- D1: 11  D2: 12  D3: 11  D4: 11  D5: 11  D6: 10  D7: 11  D8: 11  D9: 4
- 小计: 92/100
- 关键依据:
  1. `SKILL.md` L99-118「S/A 规则表」：S 级 8 条 + A 级 9 条，每条「✗ 错误 → ✓ 正确」，可作 checklist 逐条对。
  2. `SKILL.md` L156-164「DoD 锚点」三条可勾选项（四维度齐全 / 分支覆盖无盲区 / 等价类清单已显式走完），是天然的提交前 checklist。
  3. `01` L56-59 antipatterns + `05` L112-117 antipatterns 互补，覆盖更细。
- 扣分主因: D6 略扣——checklist 信息散在三处（S/A 表 + DoD 锚点 + 05/01 antipatterns），需跨文件拼。但 SKILL.md 单文件内的 S/A 表 + DoD 锚点已覆盖主体。

### 版本 B
- D1: 11  D2: 11  D3: 11  D4: 11  D5: 11  D6: 10  D7: 11  D8: 11  D9: 4
- 小计: 91/100
- 关键依据:
  1. `SKILL.md` L99-118 S/A 规则表（与 A 同结构，但 L106 @MockBean 行无 TestConfig 例外）。
  2. `01` L56-59 + `05` L112-117 antipatterns（两版相同）。
  3. `05` L90-98「三个停止信号」可作为 checklist 的"测够没"维度（在 05 文件内）。
- 扣分主因: D2 略低于 A——B 没有 SKILL.md 内的「DoD 锚点」小节，"测够没"的 checklist 要跳到 `05` L90 才能拿到（A 在 SKILL.md L156 就有可勾选 checklist，可发现性更强）。差距很小（1 分）。

---

## T4 — 快速入门（第一次接触，文件很多）

### 版本 A
- D1: 12  D2: 12  D3: 11  D4: 12  D5: 10  D6: 12  D7: 12  D8: 11  D9: 4
- 小计: 96/100
- 关键依据:
  1. `SKILL.md` L46-55「快速入门」表：4 行映射「你想做的 → 看这份 → 读多少」，新手一眼定位。
  2. `SKILL.md` L57-67「第 0 步探测」给具体 `grep -E` 命令探 pom.xml（spring-boot / junit / mockito）。
  3. `SKILL.md` L156-164「DoD 锚点」让新手知道"做完看哪"，路径闭环。
- 扣分主因: D5 偏低——快速入门面向新手，未提示"先读 01 再读 02"的依赖顺序外的隐蔽坑（但 L55 已说"01 根文件设计前必读"）。轻微。

### 版本 B
- D1: 12  D2: 12  D3: 11  D4: 12  D5: 10  D6: 12  D7: 12  D8: 11  D9: 4
- 小计: 96/100
- 关键依据:
  1. `SKILL.md` L46-55 快速入门表（与 A 同结构，唯一差异 L50 写"`references/05` 三停止信号"而非 A 的"「DoD 锚点」三停止信号"）。
  2. `SKILL.md` L57-67 第 0 步探测（与 A 相同）。
  3. `SKILL.md` L78 路由表 + L81 指针"回看 `references/05` 的三个停止信号"。
- 扣分主因: 同 A。新手在 B 下找"测够没"要跳到 05，但快速入门表已显式列出，不构成退步 → 同分。

---

## T5 — ArchUnit 跨层（Controller 直连 Repository）

### 版本 A
- D1: 12  D2: 12  D3: 12  D4: 12  D5: 11  D6: 11  D7: 12  D8: 11  D9: 4
- 小计: 97/100
- 关键依据:
  1. `SKILL.md` L124-146「架构守护询问」：触发条件精确（无 ArchUnit + 分层包 + 触达跨层依赖），并给 `grep archunit pom.xml` 判据（L128）。
  2. `SKILL.md` L130：给坐标 `com.tngtech.archunit:archunit-junit5:1.3.0` + 两个选项（引入加测试 / 不引入靠 review）。
  3. `SKILL.md` L133-144：两条可直接复制的 `@ArchTest` 规则——`webNoRepo`（抓 Controller 直连 Repository）正是本 prompt 场景；`noCycles`（分层无环）。
- 扣分主因: 无明显失分。规则代码块直接命中用户问题。

### 版本 B
- D1: 12  D2: 12  D3: 12  D4: 12  D5: 11  D6: 11  D7: 12  D8: 11  D9: 4
- 小计: 97/100
- 关键依据: 同 A（`SKILL.md` L124-146 架构守护段两版逐字节相同；ArchTest 代码块相同）。
- 扣分主因: 同 A。T5 不触及三处改动 → 同分。

---

## T6 — 停止信号可执行性（回归重点：05 刚被精简）

> rubric 特别盯：05 精简后，判定 checklist 是否仍能完整回答，还是指针断了。

### 版本 A
- D1: 11  D2: 11  D3: 12  D4: 12  D5: 11  D6: 11  D7: 11  D8: 11  D9: 4
- 小计: 94/100
- 关键依据:
  1. `SKILL.md` L156-164「测试完成判定（DoD 锚点）」：**三个可勾选项 + 每项的"怎么检查"**——L161「`mvn test` 后看 JaCoCo 报告 Branch 列，红色（`BRANCH_MISSED > 0`）行逐一对照」给出具体列名与动作；L158 显式标注"本节供外部 PR/DoD checklist 直接引用"。
  2. `SKILL.md` L78 路由表 + L81 指针"回看「DoD 锚点」三停止信号"——指针没断，且指向 SKILL.md 内（无需跨文件）。
  3. `05` L90-94：精简后只剩一句"可勾选判定 checklist 见 SKILL.md「DoD 锚点」"+ 开放集陷阱说明（"无遗漏不可绝对证明"），与 SKILL.md 锚点互补不重复。
- 扣分主因: 精简是**正向**的——判定 checklist 移到 SKILL.md 后可发现性反而提升（一处可引用），未断链。按 rubric"不应仅因内容换了位置而扣分"。唯一轻微 D2 扣分：用户若直接读 05，需跳回 SKILL.md（一步跳转，可接受）。

### 版本 B
- D1: 11  D2: 11  D3: 12  D4: 12  D5: 11  D6: 10  D7: 11  D8: 11  D9: 4
- 小计: 92/100
- 关键依据:
  1. `05` L90-98「三个停止信号」完整版：每条信号附"怎么检查"——L95「`mvn test` 后看 `target/site/jacoco/jacoco.csv` 的 `BRANCH_MISSED` 列（或 index.html 的 Branch 列红色行）」给具体文件 + 列名。
  2. `SKILL.md` L78 路由表 + L81 指针"回看 `references/05` 的三个停止信号"——指针有效。
  3. `05` L94-96 三条信号内容完整（四维度齐全 / 分支覆盖无盲区 / 等价类清单已显式走完）。
- 扣分主因: 内容完整、可执行性不输 A。但 (a) 判定 checklist 埋在 `05` 而非 SKILL.md，外部 PR/DoD 引用时需指明"见 references/05 L90"，可引用性略弱于 A 的独立锚点小节；(b) D6 略扣——`05` L92-98 与 `05` L94-96 对信号 3 的"开放集"说明有轻微重复。两版在本 prompt 下能力相当，A 略胜在"可引用锚点"，差距 2 分。

---

## T7 — AI 生成测试自欺

### 版本 A
- D1: 12  D2: 12  D3: 12  D4: 11  D5: 12  D6: 11  D7: 11  D8: 11  D9: 4
- 小计: 96/100
- 关键依据:
  1. `SKILL.md` L108 S 级「直接采信 AI 一次性生成的"四维度齐全"测试」：给可执行复核法——"代表值换一个同区间值期望行为不变（变了说明选错了类）；且至少触发一条与相邻等价类不同的分支"。
  2. `SKILL.md` L108 同时点名异常维度陷阱"断言被测契约指定的具体异常类（如 `IllegalArgumentException`），勿断言 `RuntimeException`/`Exception` 根父类"——这是 AI 生成测试的典型假绿。
  3. description L14 次级触发信号含相关场景。
- 扣分主因: 无明显失分。复核法可机械执行，陷阱具体。

### 版本 B
- D1: 12  D2: 12  D3: 12  D4: 11  D5: 12  D6: 11  D7: 11  D8: 11  D9: 4
- 小计: 96/100
- 关键依据: 同 A（`SKILL.md` L108 该 S 级行两版逐字节相同）。
- 扣分主因: 同 A。T7 不触及三处改动 → 同分。

---

## T8 — Mock 复杂场景（MyBatis-Plus lambdaQuery + 静态方法）

### 版本 A
- D1: 12  D2: 12  D3: 12  D4: 12  D5: 12  D6: 11  D7: 12  D8: 11  D9: 4
- 小计: 98/100
- 关键依据:
  1. `06` L108-124「链式调用 / 参数对象 mock（MyBatis-Plus、JPA Query）」：直接命中 `LambdaQueryWrapper` 场景，给 ✓ 代码 `when(mapper.selectOne(any(LambdaQueryWrapper.class))).thenReturn(null)`。
  2. `06` L122-124 进一步给 `ArgumentCaptor` 捕获 + `verify(mapper).selectOne(captor.capture())` 确认调用，并提醒"wrapper 内部条件断言较脆（lambda 序列化不稳）"——隐蔽坑。
  3. `06` L93-106 静态方法 mock + L126-148「重构为可注入实例」对照，覆盖 prompt 的"静态方法"子问题。
- 扣分主因: 无明显失分。范例直接可复制。

### 版本 B
- D1: 12  D2: 12  D3: 12  D4: 12  D5: 12  D6: 11  D7: 12  D8: 11  D9: 4
- 小计: 98/100
- 关键依据: 同 A（`06` L108-148 该段两版逐字节相同；唯一差异是 §1 注释里的断言触发措辞，与 Mock 范例无关）。
- 扣分主因: 同 A。T8 不触及三处改动 → 同分。

---

## T9 — 【对抗】表格型 VO 逐列断言（16 列）+ 集合断言

> rubric 判分锚点：(a) 若建议用 `extracting(16 fields).containsExactly` → D3/D5 重扣（功能性缺陷）。正确留在 JUnit 原生逐列 → 高分。(b) list 升级到 AssertJ 集合断言 → 高分。

### 版本 A
- D1: 12  D2: 12  D3: 12  D4: 12  D5: 12  D6: 11  D7: 12  D8: 11  D9: 4
- 小计: 98/100
- 关键依据:
  1. `SKILL.md` L36-41 断言库策略：升级条件明确为「集合内容断言」或「字段分组断言（**同一逻辑组**，如坐标 x/y/z、时间窗 start/end）」。**16 列 Excel VO 各列语义独立、非同一逻辑组 → 不触发升级**。L41 显式：「各列语义独立的逐列断言如表格型 VO 一律 JUnit 原生，不切换……留在原生逐列断言（失败定位准、加列只改一处）」——**直接命中 T9(a) 正解**。
  2. `SKILL.md` L41 给机械判据：「是否集合？是否同一逻辑组？两条都不是 → 留在原生逐列断言」——agent 可机械执行，不靠"感觉"。
  3. L38「集合内容断言（断言 list 元素构成/顺序/提取字段）→ AssertJ `containsExactlyInAnyOrder` / `extracting(...)`」——命中 T9(b) 正解（list 顺序校验升级到 AssertJ）。
- 扣分主因: 无功能性缺陷。(a)(b) 两问都给正解。D6 轻微扣（策略段稍长），D8 轻微扣（未显式说"16 列"这个数字，但"表格型 VO"已覆盖语义）。

### 版本 B
- D1: 11  D2: 12  D3: 5  D4: 8  D5: 4  D6: 11  D7: 11  D8: 11  D9: 3
- 小计: 76/100
- 关键依据:
  1. `SKILL.md` L38 断言库策略升级条件之一：「**同一对象需要 3 个及以上字段断言** —— AssertJ 的 `extracting(...).containsExactly(...)` 集中描述」。16 列 VO 远 ≥ 3 → **按字面执行会误判为该用 `extracting(16 fields).containsExactly(16 values)`**——这正是 rubric 点名的功能性缺陷（失败时报整个 tuple 不匹配、顺序敏感、加列要改两处、失败栈帧不指字段名）。
  2. L41「其余场景（简单值、抛异常、**1~2 个字段断言**）一律 JUnit 原生」——反向印证：B 的阈值是"字段数 ≤ 2 留原生"，3+ 即升级，与 16 列逐列断言的正确做法直接冲突。
  3. L41「升级标准可机械执行：数字段数 / 判断是否集合」——agent 会机械地"数到 16 ≥ 3 → 升级 extracting"，给出错误建议。L43-44 无任何"语义独立列不要分组"的例外。
  4. (b) 部分两版都正确：L39「集合内容断言（断言元素构成/顺序/提取字段）→ AssertJ」覆盖 list 顺序校验。
- 扣分主因: **(a) 功能性缺陷**——升级阈值"3+ 字段"会让 agent 对 16 列表格 VO 给出 `extracting(...).containsExactly(...)` 的错误建议，违背"失败定位准"诉求。按 rubric T9 锚点，D3/D5 重扣（D3=5, D5=4）。D4 扣（错误建议本身不可执行到正确结果）。D9 扣 1 分（L17 description 也写"仅在 3+ 字段断言/集合内容断言时升级到 AssertJ"，与 L38 表述一致但整体方向错）。(b) 部分正确，不额外扣。

---

## T10 — 【对抗】项目既定 mock 装配（TestConfig + @Bean mock）vs @MockBean 滥用

> rubric 判分锚点：(a) 若建议把 TestConfig 迁到 `@InjectMocks` → D3/D5 重扣（误报）。正确跟随项目 → 高分。(b) @MockBean 起全量 Context 是真反模式，两版都应正确。

### 版本 A
- D1: 12  D2: 12  D3: 12  D4: 12  D5: 12  D6: 11  D7: 12  D8: 11  D9: 4
- 小计: 98/100
- 关键依据:
  1. `SKILL.md` L106 S 级 @MockBean 行：「**项目既定的 mock 装配模式（如 TestConfig 手写 `@Bean` mock 轻量容器）优先于本默认**——跟随项目，不强加 `@InjectMocks`」。**直接命中 T10(a) 正解**——TestConfig 写法是合法装配，不是反模式，应跟随项目，回应同事的"违反规范"质疑为误报。
  2. L106 同时把红线收窄到「`@MockBean` 的红线是『用于纯单测起全量 Spring Context』（会重建 Context），切片测试才用它」——命中 T10(b) 正解（@SpringBootTest + @MockBean 起全量上下文是真反模式）。
  3. 三条铁律 L32「栈中立：测试框架跟随项目既有……不强加、不主动迁移」——为 (a) 提供 总纲支撑。
- 扣分主因: 无功能性缺陷。(a)(b) 都给正解，且 L106 是专门针对本场景写的例外条款。D6 轻微扣（L106 较长），D8 轻微扣（未显式说 @Import 但语义覆盖）。

### 版本 B
- D1: 11  D2: 12  D3: 5  D4: 8  D5: 4  D6: 11  D7: 11  D8: 11  D9: 3
- 小计: 76/100
- 关键依据:
  1. `SKILL.md` L106 S 级 @MockBean 行：「`@MockBean` 用于纯单元测试 → 纯单测用 `@Mock`+`@InjectMocks`；`@MockBean` 留给切片测试」。**没有任何关于"项目既定 TestConfig @Bean mock 装配优先"的例外**。按字面执行，agent 看到 prompt 里 TestConfig + @Import + @Autowired 真实 Service 的写法，会判定为"没遵守 @Mock+@InjectMocks 默认"，给出"应该改成 @InjectMocks"的迁移建议——这正是 rubric 点名的**误报**，违背栈中立铁律。
  2. `06` L64-72「何时改用手工构造注入」列了三个例外（多构造器 / 需真实值 / 团队既有手工构造），但**不覆盖"@Bean mock 轻量容器"这种项目级装配模式**——agent 在 B 下找不到支持 TestConfig 写法的依据。
  3. L106 后半句「`@MockBean` 留给切片测试（它会重建 Spring Context，纯单测不该起容器）」——(b) 部分正确，能拦截 @SpringBootTest + @MockBean 真反模式。
- 扣分主因: **(a) 功能性缺陷**——B 的 @MockBean S 级行只给一个正解（@Mock+@InjectMocks），无项目既定装配例外，agent 会误报 TestConfig 写法"违反规范"并建议错误迁移。按 rubric T10 锚点，D3/D5 重扣（D3=5, D5=4）。D4 扣（错误迁移建议不可执行到正确结果）。D9 扣 1 分（栈中立铁律 L32 与 L106 缺乏例外形成内部张力，agent 难两全）。(b) 部分正确，不额外扣。

---

## T11 — 【对抗】DoD 写"覆盖率 ≥ 80%"

> rubric 判分锚点：若只泛泛说"覆盖率不是目标"但不给可引用的替代判定 → D4 扣分。给出可勾选 checklist 锚点 → 高分。

### 版本 A
- D1: 12  D2: 12  D3: 12  D4: 12  D5: 12  D6: 11  D7: 12  D8: 11  D9: 4
- 小计: 98/100
- 关键依据:
  1. `SKILL.md` L156-164「测试完成判定（DoD 锚点）」：**三个可勾选项**——L160「四维度齐全」、L161「分支覆盖无盲区（看 JaCoCo Branch 列 `BRANCH_MISSED > 0`）」、L162「等价类清单已显式走完」。L158 显式标注「本节供外部 PR/DoD checklist **直接引用**」——**直接命中 T11"可被 PR 模板/checklist 引用"诉求**。
  2. `SKILL.md` L164 显式反例对照：「✗ DoD 写『覆盖率 ≥ 80%』→ ✓ DoD 写『四维度齐全 + 分支覆盖无盲区 + 等价类清单已走完』。前者把覆盖率当目标，催生凑数测试；后者才是测试设计完整性的判定。」——**逐字命中 prompt 场景**。
  3. `05` L7-9 + L114 antipattern「✗ 把『覆盖率 ≥ 80%』写进 CI 当硬门禁 → ✓ 覆盖率作报告+趋势监控，关键模块设阈值，胶水代码豁免」——为"覆盖率不是目标"提供理由。
- 扣分主因: 无明显失分。DoD 锚点小节是本次反馈3 的直接修复，可发现性、可引用性、可执行性三高。

### 版本 B
- D1: 11  D2: 9  D3: 11  D4: 8  D5: 11  D6: 10  D7: 9  D8: 11  D9: 4
- 小计: 84/100
- 关键依据:
  1. `05` L7-9「覆盖率的定位」表：明确"✗ 不是目标 / ✓ 是反向诊断"，点出"追 80% 覆盖率催生无脑凑数测试"——能回答"合不合理"。
  2. `05` L114 antipattern「✗ 把『覆盖率 ≥ 80%』写进 CI 当硬门禁 → ✓ 覆盖率作报告+趋势监控，关键模块设阈值，胶水代码豁免」——给替代方向。
  3. `05` L90-98「三个停止信号」可作为替代判定的素材，但**没有"可被外部 PR/DoD checklist 直接引用"的稳定锚点**——agent 需自己从 05 拼装（L7-9 说不是目标 + L90-98 给三信号 + L114 给替代），跨文件组装。
- 扣分主因: (a) 能说清"不合理"，但**没有一个可直接复制到 PR 模板/checklist 的成品判定**——`05` L114 的替代是"覆盖率作报告+趋势监控"（仍是覆盖率视角），而"四维度齐全 + 分支覆盖无盲区 + 等价类清单已走完"这个 checklist 形式的判定散在 L90-98 三条信号里，未标注"可外部引用"。按 rubric T11 锚点"不给可引用的替代判定 → D4 扣分"。(b) D2/D7 扣——SKILL.md 无 DoD 锚点小节，用户问"改成什么"时 agent 要跨到 05 才能拼出答案，可发现性/可引用性弱于 A 的独立锚点。(c) D9 不扣——B 内部无矛盾。

---

## 汇总表

| Prompt | 版本 A 小计 | 版本 B 小计 | 差值 (A−B) |
|---|---|---|---|
| T1  JaCoCo 集成 | 93 | 93 | 0 |
| T2  遗留代码补测 | 94 | 94 | 0 |
| T3  review checklist | 92 | 91 | +1 |
| T4  快速入门 | 96 | 96 | 0 |
| T5  ArchUnit 跨层 | 97 | 97 | 0 |
| T6  停止信号可执行性 | 94 | 92 | +2 |
| T7  AI 生成测试自欺 | 96 | 96 | 0 |
| T8  Mock 复杂场景 | 98 | 98 | 0 |
| **T9  表格 VO 逐列断言（对抗）** | **98** | **76** | **+22** |
| **T10 TestConfig vs @MockBean（对抗）** | **98** | **76** | **+22** |
| **T11 DoD 覆盖率 ≥ 80%（对抗）** | **98** | **84** | **+14** |
| **总计 (/1100)** | **1054** | **993** | **+61** |

---

## 核心结论

1. **回归集 T1-T8 未退步**：版本 A 在 T1/T2/T4/T5/T7/T8 与版本 B 同分（触及的 reference 两版逐字节相同）；T3（+1）、T6（+2）版本 A 略高，均来自 SKILL.md 新增的「DoD 锚点」小节带来的可发现性/可引用性提升，而非 B 退步。**三处改动（断言范围 / @MockBean 装配 / DoD 锚点+05 精简）未让任何回归场景退步**。

2. **对抗集 T9/T10/T11 差异显著且符合 rubric 预期**：
   - **T9（+22）**：版本 A 的断言升级条件改为"集合内容 / 字段分组断言（同一逻辑组）"，16 列语义独立 VO 不触发升级 → 正解留在 JUnit 原生逐列；版本 B 的"3+ 字段"阈值会让 agent 误判为该用 `extracting(16 fields).containsExactly`——功能性缺陷，D3/D5 重扣。
   - **T10（+22）**：版本 A 的 @MockBean S 级行新增"项目既定 mock 装配（TestConfig @Bean mock 轻量容器）优先于本默认"例外 → 正解跟随项目；版本 B 无此例外，agent 会误报 TestConfig 写法"违反规范"并建议错误迁移到 @InjectMocks——功能性缺陷（误报，违背栈中立），D3/D5 重扣。
   - **T11（+14）**：版本 A 的 SKILL.md「DoD 锚点」小节给三条可勾选 checklist + 显式反例（✗ 覆盖率 ≥ 80% → ✓ 三条判定），可直接被 PR/checklist 引用；版本 B 能说清"不合理"但无可引用的成品判定，agent 需跨文件拼装——按 rubric D4/D2/D7 扣分。

3. **总评**：版本 A 总分 1054，版本 B 总分 993，差 +61。差距几乎全部来自三条对抗集（+58/61），回归集仅贡献 +3/61。这与本次验证"三处改动修掉三条反馈根因、且不伤回归场景"的设计意图高度吻合。
