# 盲评结果（blind-A）

> 独立盲评，按 `rubric.md` 9 维度打分。两版元数据：**版本1** = `version1/`（其 SKILL.md frontmatter `version: "1.3.2"`），**版本2** = `version2/`（frontmatter `version: "1.4.0"`）。元数据不影响打分。
>
> **版本差异定位**（先用 diff 锁定两版真正不同的位置，再据此打分）：
> - `01/02/03/04` 四份 reference 两版**逐字相同**（diff 无输出）。
> - `06-tools-lean.md` 两版**逐字相同**，仅 §1 注释里"3+字段断言" vs "集合内容/字段分组断言（同一逻辑组）"措辞随 SKILL 同步（version1 L15 vs version2 L15）。
> - **真正不同的只有 `SKILL.md` 和 `05-coverage-and-quantity.md` 两份**，集中在三处：
>   1. **断言库策略**：version1 SKILL.md L17/L38 "同一对象需要 3 个及以上字段断言 → extracting"；version2 SKILL.md L17/L39/L41 改为"字段分组断言（同一逻辑组，如坐标 x/y/z）"+ 显式"各列语义独立的逐列断言如表格型 VO 一律 JUnit 原生（失败定位准、加列只改一处）"。
>   2. **S 表 @MockBean 行**：version1 SKILL.md L106 只给唯一正解"纯单测用 @Mock+@InjectMocks"；version2 SKILL.md L106 补"项目既定的 mock 装配模式（如 TestConfig 手写 @Bean mock 轻量容器）优先于本默认——跟随项目，不强加 @InjectMocks"，并把红线收窄为"@MockBean 用于纯单测起全量 Spring Context"。
>   3. **DoD 锚点**：version1 无此小节，停止信号全在 05 L90-98（3 条信号+各带"怎么检查"）；version2 SKILL.md L156-164 新增"测试完成判定（DoD 锚点）"小节（3 个可勾选项 + ✗覆盖率≥80% → ✓三条判定 + 标注"供外部 PR/DoD checklist 直接引用"），05 L90-94 相应精简为指针 + 留"无遗漏不可绝对证明"判定陷阱。
>
> 故 **T1/T2/T3/T5/T7/T8 六条回归 prompt 两版相关内容完全相同，预期同分**；T4/T6 有"内容换了位置/措辞"的轻微差异；T9/T10/T11 三条对抗 prompt 差异集中在上述三处。

---

## T1 — JaCoCo 集成

### 版本1
- D1: 10  D2: 11  D3: 11  D4: 12  D5: 11  D6: 11  D7: 11  D8: 11  D9: 4
- 小计: **92/100**
- 关键依据:
  1. `05-coverage-and-quantity.md` L38-58 给出可复制的 JaCoCo `<plugin>` 完整 XML（jacoco-maven-plugin 0.8.12 + prepare-agent + report execution）。
  2. L60 "`mvn test` 后看 `target/site/jacoco/index.html`"——可执行命令 + 报告路径齐全。
  3. L62 坑提示"测试不过时报告不生成"+ 解法（改 phase=verify 或 `mvn jacoco:report`）；06 §5 L214-216 也明确"仅用户问覆盖率时才提"。
- 扣分主因: 无明显失分。覆盖率为反向诊断的定位（L3-9）略偏说理但属必要。

### 版本2
- D1: 10  D2: 11  D3: 11  D4: 12  D5: 11  D6: 11  D7: 11  D8: 11  D9: 4
- 小计: **92/100**
- 关键依据: 与版本1 完全相同（`05` 的 JaCoCo 段、06 §5 两版逐字一致）。
- 扣分主因: 同版本1。

---

## T2 — 遗留代码补测

### 版本1
- D1: 11  D2: 11  D3: 12  D4: 11  D5: 12  D6: 11  D7: 11  D8: 11  D9: 4
- 小计: **94/100**
- 关键依据:
  1. `05` L100-114「遗留代码：先特征测试锁现状，再小步重构」给出固定解法顺序：特征测试录现状 → 重构（new→构造器注入、static→接口注入）→ 再补四维度。
  2. `05` L106 特征测试具体写法 `assertEquals(currentActual, service.doX(input))`；L110 切片策略"挑变更频率×业务风险最高的单方法先闭环"。
  3. L113-114 明确"禁止 PowerMock/反射强 mock"，根因治本靠注入重构；06 §3 补 static mock + new 依赖头号陷阱。
- 扣分主因: 无明显失分。

### 版本2
- D1: 11  D2: 11  D3: 12  D4: 11  D5: 12  D6: 11  D7: 11  D8: 11  D9: 4
- 小计: **94/100**
- 关键依据: 与版本1 完全相同（`05` 遗留代码段两版逐字一致）。
- 扣分主因: 同版本1。

---

## T3 — review checklist

### 版本1
- D1: 11  D2: 11  D3: 11  D4: 11  D5: 11  D6: 11  D7: 10  D8: 11  D9: 4
- 小计: **91/100**
- 关键依据:
  1. `SKILL.md` L99-117「S/A 规则表」即现成 review 清单：S 级 8 条（happy path/想到几个写几个/多条件漏组合/状态机漏非法转移/new 依赖被 mock/@MockBean 滥用/mockStatic 未 try/AI 生成未复核）+ A 级 9 条。
  2. L83-92「四维度必检清单」表（正向/反向/边界/异常）作另一自查维度。
  3. L119-122「Mock 边界」点出"只 mock 外部依赖"。
- 扣分主因: 清单分布在 S/A 表 + 四维度表 + Mock 边界三处，需 agent 自行聚合（D7 略压）。

### 版本2
- D1: 11  D2: 11  D3: 11  D4: 11  D5: 11  D6: 11  D7: 10  D8: 11  D9: 4
- 小计: **91/100**
- 关键依据: S/A 表、四维度表、Mock 边界两版逐字相同（仅 L106 @MockBean 行措辞不同，对 T3 清单功能无影响）。
- 扣分主因: 同版本1。

---

## T4 — 快速入门

### 版本1
- D1: 11  D2: 12  D3: 11  D4: 11  D5: 10  D6: 11  D7: 12  D8: 11  D9: 3
- 小计: **92/100**
- 关键依据:
  1. `SKILL.md` L46-55「快速入门」表 4 行直接给"你想做的→看这份→读多少"映射（01/02/03-04/06）。
  2. L48 第一行写"`references/01` 四维度 + `references/05` 三停止信号"——但版本1 SKILL.md 无"DoD 锚点"小节名，此处的"三停止信号"指向 05（指针有效但措辞与文件内小节名不完全对齐）。
  3. L148-154「使用流程」5 步给完整动线。
- 扣分主因: L48 引"三停止信号"指 05，而 05 内小节名为"什么时候算'够了'——三个停止信号"，跨文件指代略松（D9 -1）。

### 版本2
- D1: 11  D2: 12  D3: 11  D4: 11  D5: 10  D6: 11  D7: 12  D8: 11  D9: 4
- 小计: **93/100**
- 关键依据:
  1. `SKILL.md` L46-55「快速入门」表结构与版本1 一致；L48 第一行改为"`references/01` 四维度 + 「DoD 锚点」三停止信号"。
  2. L156 存在同名小节「测试完成判定（DoD 锚点）」，L48 引用名与文件内小节名严格对齐，指代无歧义。
  3. L148-154「使用流程」5 步与版本1 相同。
- 扣分主因: 无明显失分。比版本1 +1 在 D9（引用名与小节名一致）。

---

## T5 — ArchUnit 跨层

### 版本1
- D1: 11  D2: 11  D3: 11  D4: 12  D5: 10  D6: 11  D7: 11  D8: 11  D9: 4
- 小计: **92/100**
- 关键依据:
  1. `SKILL.md` L124-146「架构守护询问」段：L126 触发条件精确（用户要求防跨层 + 项目无 ArchUnit；或被测类在分层包且触达跨层依赖）；L128 `grep archunit pom.xml` 判定法。
  2. L130 给坐标 `com.tngtech.archunit:archunit-junit5:1.3.0` + 两个选项（引入加架构测试 / 不引入靠人工 review）。
  3. L133-143 给两条**可直接复制**规则：`webNoRepo`（抓 Controller 直连 Repository）+ `noCycles`（分层无环），正好命中本 prompt 的"Controller 直接调 Repository"。
- 扣分主因: 无明显失分。

### 版本2
- D1: 11  D2: 11  D3: 11  D4: 12  D5: 10  D6: 11  D7: 11  D8: 11  D9: 4
- 小计: **92/100**
- 关键依据: 架构守护段两版逐字相同（L124-146）。
- 扣分主因: 同版本1。

---

## T6 — 停止信号可执行性（回归重点：05 刚被精简）

### 版本1
- D1: 11  D2: 10  D3: 12  D4: 11  D5: 11  D6: 11  D7: 10  D8: 11  D9: 4
- 小计: **91/100**
- 关键依据:
  1. `05` L90-98「什么时候算'够了'——三个停止信号」完整内嵌 3 条信号，每条带"怎么检查"：信号1"对着用例清单逐维度打勾"+"真代表性查 02"；信号2"`mvn test` 后看 `target/site/jacoco/jacoco.csv` 的 `BRANCH_MISSED` 列（或 `index.html` 的 Branch 列红色行）"；信号3"按 02 四步法列出有效/无效等价类清单，逐类打勾"。
  2. L94-96 三条同时满足 = 最小充分集；L96 点"无遗漏不可绝对证明（开放集）"判定陷阱。
  3. SKILL.md L78/L81 路由表 + 提示"每次设计完用例后都应回看 `references/05` 的三个停止信号"。
- 扣分主因: 答案埋在 05 二级文件，需从 SKILL 跳一次（D2/D7 略压）。

### 版本2
- D1: 11  D2: 11  D3: 12  D4: 11  D5: 11  D6: 11  D7: 11  D8: 11  D9: 4
- 小计: **93/100**
- 关键依据:
  1. `SKILL.md` L156-164「测试完成判定（DoD 锚点）」直接在主文件给 3 个可勾选项：四维度齐全 / 分支覆盖无盲区 / 等价类清单已显式走完——L161 保留可执行动作"所有 if/switch 的每个分支都被至少一个用例走到——`mvn test` 后看 JaCoCo 报告的 Branch 列，红色（`BRANCH_MISSED > 0`）行逐一对照……**项目无 JaCoCo 时**：人工核对分支表，或按'成本收益'决定是否接入（接入见 05）"。
  2. L50 快速入门表 + L78 路由表 + L81 提示均改名指向「DoD 锚点」，导航一致。
  3. `05` L90-94 精简为指针"可勾选的判定 checklist 见 SKILL.md「测试完成判定（DoD 锚点）」"，并保留 DoD 锚点未承载的判定陷阱（L94"无遗漏不可绝对证明"）。反向指针完整，无断链。
- 扣分主因: 05 信号1 原有的"真代表性查 02"细化被收敛（DoD 锚点 L160 改为"对照四维度表逐项打勾"），细节略减但可执行性保留。按 rubric"通过 DoD 锚点仍能完整回答 T6（甚至更易引用）→ 不应仅因换了位置而扣分"，整体不退步、反因放主文件更易发现（D2/D7 +1）。

---

## T7 — AI 生成测试自欺

### 版本1
- D1: 11  D2: 11  D3: 11  D4: 11  D5: 12  D6: 11  D7: 10  D8: 11  D9: 4
- 小计: **92/100**
- 关键依据:
  1. `SKILL.md` L108 S 表「直接采信 AI 一次性生成的'四维度齐全'测试」行：✗ → ✓"必须人工复核三维度的**真代表性**：代表值换一个同区间值期望行为不变（变了说明选错了类）；且至少触发一条与相邻等价类不同的分支。异常维度断言**被测契约指定的具体异常类**，勿断言 `RuntimeException`/`Exception` 根父类"。
  2. `05` L116-121 覆盖率反模式（"为了凑覆盖率写无断言测试"等）补充风险面。
  3. `description`（L14-16）次级触发信号含"@MockBean 用于纯单测"等，能识别此类风险 prompt。
- 扣分主因: 复核动作分布在 S 表 + 05，需 agent 聚合（D7 略压）。

### 版本2
- D1: 11  D2: 11  D3: 11  D4: 11  D5: 12  D6: 11  D7: 10  D8: 11  D9: 4
- 小计: **92/100**
- 关键依据: L108 S 表 AI 行两版逐字相同；05 反模式段相同。
- 扣分主因: 同版本1。

---

## T8 — Mock 复杂场景

### 版本1
- D1: 11  D2: 11  D3: 12  D4: 12  D5: 11  D6: 11  D7: 11  D8: 11  D9: 4
- 小计: **94/100**
- 关键依据:
  1. `06` L108-124「链式调用 / 参数对象 mock（MyBatis-Plus、JPA Query）」直接命中：被测方内部 `new LambdaQueryWrapper().eq(...).eq(...)` 再 `selectOne(wrapper)` → 用 `when(mapper.selectOne(any(LambdaQueryWrapper.class))).thenReturn(null)` + 完整范例代码。
  2. L122-124 补强：`any()` 会放过 wrapper 自身构造 bug → 用 `ArgumentCaptor` + `verify(mapper).selectOne(captor.capture())`。
  3. L93-106「静态方法 mock（最后手段）」给 `mockStatic` + try-with-resources 范例；L126-148「静态方法：重构为可注入实例」给 IdGenerator/Clock 注入前后对照（命中"工具类静态方法"）。
- 扣分主因: 无明显失分。

### 版本2
- D1: 11  D2: 11  D3: 12  D4: 12  D5: 11  D6: 11  D7: 11  D8: 11  D9: 4
- 小计: **94/100**
- 关键依据: 06 §3 链式调用段、静态方法段两版逐字相同。
- 扣分主因: 同版本1。

---

## T9 — 【对抗 · 反馈1 根因】表格型 VO 逐列断言 + 集合断言

### 版本1
- D1: 10  D2: 11  D3: 4  D4: 4  D5: 3  D6: 11  D7: 11  D8: 11  D9: 4
- 小计: **69/100**
- 关键依据:
  1. **(a) 致命误导**：`SKILL.md` L17 description + L36-38「断言库策略」写"**同一对象需要 3 个及以上字段断言** —— AssertJ 的 `extracting(...).containsExactly(...)` 集中描述"；L41"升级标准可机械执行：**数字段数** / 判断是否集合"。16 列 VO 字段数 16 ≥ 3 → 按字面机械执行**会触发升级**，建议 `extracting(16 fields).containsExactly(16 values)`。这正是 rubric 点名的功能性缺陷：失败时报整个 tuple 不匹配、顺序敏感、加列要改两处，与用户"失败定位准"诉求直接矛盾。
  2. (a) 无任何反例/例外把"各列语义独立的表格 VO"留在原生逐列断言——规则一刀切按字段数。
  3. **(b) 正确**：L39"集合内容断言（断言元素构成/顺序/提取字段）→ AssertJ 的 `containsExactlyInAnyOrder` / `extracting(...)`"——list 个数+顺序校验升级到 AssertJ 集合断言，两版都答对。
- 扣分主因: (a) 给出错误建议（extracting 塞 16 列），按 rubric 对抗集铁律 D3/D5/D4 重扣（≤4）。这是反馈1 的真实痛点，不可因文字通顺给中庸分。

### 版本2
- D1: 12  D2: 12  D3: 12  D4: 12  D5: 12  D6: 11  D7: 11  D8: 11  D9: 4
- 小计: **97/100**
- 关键依据:
  1. **(a) 正确拦截**：`SKILL.md` L17 description 改为"仅在**集合内容断言/字段分组断言（同一逻辑组）**时升级"；L39"**字段分组断言（同一逻辑组的多个字段作为一个整体校验，如坐标 x/y/z、时间窗 start/end）**"；L41 显式"**各列语义独立的逐列断言如表格型 VO 一律 JUnit 原生，不切换**。升级标准可机械执行：是否集合？是否同一逻辑组？两条都不是 → 留在原生逐列断言（**失败定位准、加列只改一处**）"——16 列语义独立、非同一逻辑组 → 不触发升级，正确留在 JUnit 原生逐列断言，且"失败定位准"直接命中用户诉求。
  2. L39 给的反例（坐标 x/y/z、时间窗 start/end）正好划清"同一逻辑组"边界，可机械判别。
  3. **(b) 正确**：L38"集合内容断言（断言 list 元素构成/顺序/提取字段）→ `containsExactlyInAnyOrder` / `extracting(...)`"——list 校验升级到 AssertJ，与 (a) 区分清晰。
- 扣分主因: 无功能性失分。仅 D6 -1（DoD 锚点 + 断言策略两处都强调"失败定位准"，轻微重复，但不构成冗余说理）。

---

## T10 — 【对抗 · 反馈2 根因】项目既定 mock 装配 vs @MockBean 滥用

### 版本1
- D1: 11  D2: 11  D3: 5  D4: 5  D5: 5  D6: 11  D7: 11  D8: 11  D9: 4
- 小计: **74/100**
- 关键依据:
  1. **(a) 误报风险**：`SKILL.md` L106 S 表 `@MockBean` 行的 ✓ 正解只有一句"纯单测用 `@Mock`+`@InjectMocks`；`@MockBean` 留给切片测试"——**无任何"项目既定 mock 装配优先"例外**。用户的 TestConfig（`@Bean` mock + `@Import` + `@Autowired` 真实 Service）既不是 `@MockBean` 也不是纯 `@InjectMocks`，按字面执行 agent 会建议"改成 @InjectMocks"，正是 rubric 点名的误报（违背"栈中立"铁律）。
  2. `06` §3 L66-71「何时改用手工构造注入」只列多构造器/真实值/"团队既有测试统一用手工构造 → 跟随"——但 TestConfig+@Bean mock 是 Spring 轻量容器装配（`@Import`+`@Autowired`），**不等于**"手工 `new` 构造"，三条都不命中，无法为 TestConfig 写法背书。
  3. **(b) 正确**：L106"@MockBean 会重建 Spring Context，纯单测不该起容器" + 06 L192-197 `@MockBean` vs `@Mock` 表（开销"启动/重建 Spring Context"）→ 正确拦截"@MockBean 起全量上下文"真反模式。
- 扣分主因: (a) 会误报 TestConfig 写法、给错误迁移建议，按 rubric D3/D5/D4 重扣。06 L70"团队既有……跟随"是弱信号但覆盖不到 TestConfig 这类容器装配模式。

### 版本2
- D1: 12  D2: 12  D3: 12  D4: 12  D5: 12  D6: 11  D7: 11  D8: 11  D9: 4
- 小计: **97/100**
- 关键依据:
  1. **(a) 正确跟随项目**：`SKILL.md` L106 S 表 `@MockBean` 行补"✓ 纯单测默认 `@Mock`+`@InjectMocks`；**项目既定的 mock 装配模式（如 TestConfig 手写 `@Bean` mock 轻量容器）优先于本默认——跟随项目，不强加 `@InjectMocks`**"——直接背书用户的 TestConfig 写法是合法项目既定模式，不是反模式，正确回应"同事质疑是误报"。
  2. L106 同时把红线收窄为"`@MockBean` 的红线是'**用于纯单测起全量 Spring Context**'（会重建 Context），切片测试才用它"——精确区分"轻量 TestConfig"（OK）与"@MockBean 全量 Context"（反模式）。
  3. **(b) 正确**：收窄后的红线 + 06 `@MockBean` vs `@Mock` 表 → 正确拦截"纯单测 @SpringBootTest+@MockBean 起全量上下文"。
- 扣分主因: 无功能性失分。

---

## T11 — 【对抗 · 反馈3 根因】DoD 写"覆盖率 ≥ 80%"

### 版本1
- D1: 11  D2: 8  D3: 10  D4: 10  D5: 10  D6: 11  D7: 8  D8: 11  D9: 4
- 小计: **83/100**
- 关键依据:
  1. `05` L8"✗ 不是目标 | 追'80% 覆盖率'催生无脑凑数测试"正确指出反模式；L118 反模式"✗ 把'覆盖率 ≥ 80%'写进 CI 当硬门禁 → ✓ **覆盖率作报告+趋势监控，关键模块设阈值，胶水代码豁免**"——但这个 ✓ 替代是"如何使用覆盖率"，**不是**一份可被 PR checklist 直接引用的"完成判定"。
  2. `05` L90-98「三个停止信号」实际构成可执行 checklist（四维度齐全 / 分支覆盖无盲区 / 等价类清单走完），每条带"怎么检查"——替代判定**存在且可执行**，所以 D4 不算低。
  3. 但 SKILL.md **无独立 DoD 锚点小节**，三信号只在 05 二级文件；agent 需自己跨文件组装（05 的三信号 + 05 L118 的反例），**没有一个稳定、可被外部 checklist 直接引用的锚点**——rubric 点名的 D2/D7 弱点。
- 扣分主因: 替代判定可执行（D4=10 不低），但缺独立可引用锚点，可发现性/可引用性弱（D2/D7=8）。

### 版本2
- D1: 12  D2: 12  D3: 12  D4: 12  D5: 11  D6: 11  D7: 12  D8: 11  D9: 4
- 小计: **97/100**
- 关键依据:
  1. `SKILL.md` L156-164「测试完成判定（DoD 锚点）」L158 开宗明义"本节供外部 PR/DoD checklist **直接引用**"，正是 rubric 要求的可引用锚点。
  2. L160-162 三个**可勾选项**（`- [ ]`）：四维度齐全（对照四维度表逐项打勾）/ 分支覆盖无盲区（`mvn test` 看 JaCoCo Branch 列 `BRANCH_MISSED > 0`，项目无 JaCoCo 时人工核对）/ 等价类清单已显式走完（按 02 四步法）。每条带可执行动作，D4 满分。
  3. L164 显式反例锚点"✗ DoD 写'覆盖率 ≥ 80%' → ✓ DoD 写'四维度齐全 + 分支覆盖无盲区 + 等价类清单已走完'。前者把覆盖率当目标催生凑数测试；后者才是测试设计完整性的判定"——直接命中本 prompt 的"改成什么"诉求，且一行可贴进 PR 模板。
- 扣分主因: 无功能性失分。

---

## 汇总表

| Prompt | 版本1 小计 | 版本2 小计 | 差值（版本2 − 版本1） |
|--------|-----------:|-----------:|----------------------:|
| T1  JaCoCo 集成                | 92 | 92 | 0   |
| T2  遗留代码补测               | 94 | 94 | 0   |
| T3  review checklist          | 91 | 91 | 0   |
| T4  快速入门                  | 92 | 93 | +1  |
| T5  ArchUnit 跨层             | 92 | 92 | 0   |
| T6  停止信号可执行性（回归重点）| 91 | 93 | +2  |
| T7  AI 生成测试自欺            | 92 | 92 | 0   |
| T8  Mock 复杂场景             | 94 | 94 | 0   |
| T9  【对抗】表格 VO 逐列断言   | 69 | 97 | +28 |
| T10 【对抗】TestConfig/@MockBean| 74 | 97 | +23 |
| T11 【对抗】DoD 覆盖率≥80%     | 83 | 97 | +14 |
| **合计（满分 1100）**         | **964** | **1032** | **+68** |

### 结论性观察（基于行号证据，非版本号推测）

1. **回归集 T1-T8 无退步**：六条（T1/T2/T3/T5/T7/T8）两版相关 reference 逐字相同 → 同分；T4 仅 +1（引用名与小节名对齐，D9）；T6 +2（rubric 明示"通过 DoD 锚点仍能完整回答、更易引用则不扣分"，版本2 把判定 checklist 上提到主文件 + 反向指针完整无断链 → D2/D7 +1 各）。即 v1.4.0 三处改动**未让原场景退步**，T6 反而略改善。

2. **对抗集 T9/T10/T11 全部命中预期差异，且为总分差的主因（+65 / 占 +68 的 96%）**：
   - **T9 (a)**：版本1 SKILL.md L38"3+ 字段断言 → extracting"对 16 列 VO 会误触发升级（功能性缺陷，D3/D5/D4 ≤4）；版本2 L39/L41 改"字段分组（同一逻辑组）+ 表格型 VO 留原生"正确拦截。差 +28。
   - **T10 (a)**：版本1 SKILL.md L106 只给唯一正解 `@InjectMocks`，对 TestConfig+@Bean mock 会误报；版本2 L106 补"项目既定 mock 装配优先于本默认"正确跟随项目。差 +23。
   - **T11**：版本1 无独立 DoD 锚点，三信号埋在 05、缺可引用锚点（D2/D7=8）；版本2 SKILL.md L156-164 新增可勾选 DoD 锚点 + 显式 ✗覆盖率≥80% → ✓三条判定，可直接贴进 PR。差 +14。

3. **两版共同强项**：四维度模型、决策表/状态机设计法、06 的 MyBatis-Plus wrapper mock + ArgumentCaptor、mockStatic try-with-resources、@MockBean vs @Mock 区分——这些 reference 两版逐字一致，质量都很高（T2/T8 两版均 94）。

4. **两版共同弱项（非版本差异）**：review checklist（T3）分散在 S/A 表 + 四维度表 + Mock 边界三处需 agent 聚合（D7=10）；覆盖率"为什么是反向诊断"的说理段落偏长（D6 普遍 11）。这些不构成版本间差异。
